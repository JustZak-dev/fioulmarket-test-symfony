{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fioulmarket - Test V2</h1>

    <div class="gallery cf">
        {% for image in images %}
            <div>
                <img src="{{ image }}" title="{{ image }}" alt="{{ image }}"/>
            </div>
        {% endfor %}
    </div>
{% endblock %}